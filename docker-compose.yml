services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: daily-task-tracker
    restart: always
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - NODE_ENV=production
    volumes:
      - ./prisma/data:/app/prisma
